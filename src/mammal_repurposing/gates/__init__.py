"""Physical kill-criterion gates applied between Cluster B (ADMET) and Fusion.

Per the v2 architecture: gates are PHYSICAL kill criteria, not relative
rankings. They drop compounds entirely from the rank-fusion pool. Distinct
from soft scoring features.
"""
